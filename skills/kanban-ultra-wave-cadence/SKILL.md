---
name: kanban-ultra-wave-cadence
description: "Board-burn pattern with ultra-code: implement card -> spawn review wave -> while wave runs, implement NEXT card -> when wave lands, fix confirmed findings, gate, promote, spawn next wave. Keeps reviewer tokens and implementer time overlapped; never let a wave sit idle while you wait on it."
license: GPL-3.0
metadata:
  origin: session-mycology-promotion
  promoted-from-spore: kanban-ultra-wave-cadence
  recurrence: 1
---

# Skill: kanban-ultra-wave-cadence

## Goal
Board-burn pattern with ultra-code: implement card -> spawn review wave -> while wave runs, implement NEXT card -> when wave lands, fix confirmed findings, gate, promote, spawn next wave. Keeps reviewer tokens and implementer time overlapped; never let a wave sit idle while you wait on it.

## Use This Skill When
- The same pattern or failure mode has recurred enough to deserve a named protocol.
- The current task clearly matches the lesson captured by this promoted spore.

## Do Not Use This Skill When
- The situation is obviously unrelated to kanban-ultra-wave-cadence.
- You only have a one-off glitch with no evidence that the recurring pattern applies.

## Inputs
- The current task context.
- The relevant files, logs, or artifacts that exhibit the pattern.

## Steps
1. Verify the current task really matches the recurring pattern.
2. Apply the core lesson from the originating spore: Board-burn pattern with ultra-code: implement card -> spawn review wave -> while wave runs, implement NEXT card -> when wave lands, fix confirmed findings, gate, promote, spawn next wave. Keeps reviewer tokens and implementer time overlapped; never let a wave sit idle while you wait on it.
3. Prefer concrete evidence over narrative momentum.
4. If the pattern no longer fits reality, update or retire this skill instead of forcing it.

## Output
- A truthful, concrete application of the pattern to the current task.
