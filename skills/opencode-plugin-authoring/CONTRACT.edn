(skill-contract
  (name "opencode-plugin-authoring")
  (v "ημ.skill/opencode-plugin-authoring@0.1.0")

  (intent "Skill: OpenCode Plugin Authoring")

  (activation
    (priority 50)
    (explicit ["skill:opencode-plugin-authoring"])
    (triggers ["opencode plugin authoring"]))

  (governance
    (touch-layer :mutable)
    (non-override [:mission :directives :safety :license :output-shape])
    (requires-user-approval false))
)
