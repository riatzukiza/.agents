(skill-contract
  (name "nx-integration")
  (v "ημ.skill/nx-integration@0.1.0")

  (intent "Skill: Nx Integration")

  (activation
    (priority 50)
    (explicit ["skill:nx-integration"])
    (triggers ["nx integration"]))

  (governance
    (touch-layer :mutable)
    (non-override [:mission :directives :safety :license :output-shape])
    (requires-user-approval false))
)
