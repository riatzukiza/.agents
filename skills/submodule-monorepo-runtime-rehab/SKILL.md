---
name: submodule-monorepo-runtime-rehab
description: "Normalize newly-submoduled packages for runtime use by fixing relative tsconfig extends, workspace path assumptions, auth/env defaults, writable-temp assumptions, and storage collection collisions before compose startup."
license: GPL-3.0
metadata:
  origin: session-mycology-promotion
  promoted-from-spore: submodule-monorepo-runtime-rehab
  recurrence: 1
---

# Skill: submodule-monorepo-runtime-rehab

## Goal
Normalize newly-submoduled packages for runtime use by fixing relative tsconfig extends, workspace path assumptions, auth/env defaults, writable-temp assumptions, and storage collection collisions before compose startup.

## Use This Skill When
- The same pattern or failure mode has recurred enough to deserve a named protocol.
- The current task clearly matches the lesson captured by this promoted spore.

## Do Not Use This Skill When
- The situation is obviously unrelated to submodule-monorepo-runtime-rehab.
- You only have a one-off glitch with no evidence that the recurring pattern applies.

## Inputs
- The current task context.
- The relevant files, logs, or artifacts that exhibit the pattern.

## Steps
1. Verify the current task really matches the recurring pattern.
2. Apply the core lesson from the originating spore: Normalize newly-submoduled packages for runtime use by fixing relative tsconfig extends, workspace path assumptions, auth/env defaults, writable-temp assumptions, and storage collection collisions before compose startup.
3. Prefer concrete evidence over narrative momentum.
4. If the pattern no longer fits reality, update or retire this skill instead of forcing it.

## Output
- A truthful, concrete application of the pattern to the current task.
