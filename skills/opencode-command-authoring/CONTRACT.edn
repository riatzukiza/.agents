(skill-contract
  (name "opencode-command-authoring")
  (v "ημ.skill/opencode-command-authoring@0.1.0")

  (intent "Skill: OpenCode Command Authoring")

  (activation
    (priority 50)
    (explicit ["skill:opencode-command-authoring"])
    (triggers ["opencode command authoring"]))

  (governance
    (touch-layer :mutable)
    (non-override [:mission :directives :safety :license :output-shape])
    (requires-user-approval false))
)
