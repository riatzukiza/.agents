(skill-contract
  (name "pm2-process-management")
  (v "ημ.skill/pm2-process-management@0.1.0")

  (intent "Skill: PM2 Process Management")

  (activation
    (priority 50)
    (explicit ["skill:pm2-process-management"])
    (triggers ["pm2 process management"]))

  (governance
    (touch-layer :mutable)
    (non-override [:mission :directives :safety :license :output-shape])
    (requires-user-approval false))
)
