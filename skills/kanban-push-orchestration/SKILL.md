---
name: kanban-push-orchestration
description: "Orchestrating multi-card kanban pushes with subagents: per card, dispatch implementation agent with discovery-grounded prompt (baseline test counts, house rules, verification gates), then independent review agent (adversarial, claim-by-claim), bounce nits back to the SAME agent session via task_id resume (it has full context), commit only after review passes. Recover uncommitted agent work lost to accidental git checkout by resuming the agent session — it remembers its own changes."
license: GPL-3.0
metadata:
  origin: session-mycology-promotion
  promoted-from-spore: kanban-push-orchestration
  recurrence: 1
---

# Skill: kanban-push-orchestration

## Goal
Orchestrating multi-card kanban pushes with subagents: per card, dispatch implementation agent with discovery-grounded prompt (baseline test counts, house rules, verification gates), then independent review agent (adversarial, claim-by-claim), bounce nits back to the SAME agent session via task_id resume (it has full context), commit only after review passes. Recover uncommitted agent work lost to accidental git checkout by resuming the agent session — it remembers its own changes.

## Use This Skill When
- The same pattern or failure mode has recurred enough to deserve a named protocol.
- The current task clearly matches the lesson captured by this promoted spore.

## Do Not Use This Skill When
- The situation is obviously unrelated to kanban-push-orchestration.
- You only have a one-off glitch with no evidence that the recurring pattern applies.

## Inputs
- The current task context.
- The relevant files, logs, or artifacts that exhibit the pattern.

## Steps
1. Verify the current task really matches the recurring pattern.
2. Apply the core lesson from the originating spore: Orchestrating multi-card kanban pushes with subagents: per card, dispatch implementation agent with discovery-grounded prompt (baseline test counts, house rules, verification gates), then independent review agent (adversarial, claim-by-claim), bounce nits back to the SAME agent session via task_id resume (it has full context), commit only after review passes. Recover uncommitted agent work lost to accidental git checkout by resuming the agent session — it remembers its own changes.
3. Prefer concrete evidence over narrative momentum.
4. If the pattern no longer fits reality, update or retire this skill instead of forcing it.

## Output
- A truthful, concrete application of the pattern to the current task.
