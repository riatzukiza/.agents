(skill-contract
  (name "opencode-sdk")
  (v "ημ.skill/opencode-sdk@0.1.0")

  (intent "Skill: OpenCode SDK Workflows")

  (activation
    (priority 50)
    (explicit ["skill:opencode-sdk"])
    (triggers ["opencode sdk"]))

  (governance
    (touch-layer :mutable)
    (non-override [:mission :directives :safety :license :output-shape])
    (requires-user-approval false))
)
