(skill-contract
  (name "kanban-curation")
  (v "ημ.skill/kanban-curation@0.2.0")

  (intent
    "Normalize task/spec frontmatter for the devel/packages/kanban tool: canonical status tokens, tags vs labels, and priority/urgency fields.")

  (activation
    (priority 60)
    (explicit ["skill:kanban-curation"])
    (triggers ["normalize kanban" "frontmatter" "status tokens" "labels to tags" "eta-mu-board"]))

  (governance
    (touch-layer :mutable)
    (non-override [:mission :directives :safety :license :output-shape])
    (requires-user-approval false))

  (effects
    (writes true)
    (network false)
    (commits false))
)
