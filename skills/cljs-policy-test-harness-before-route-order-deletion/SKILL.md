---
name: cljs-policy-test-harness-before-route-order-deletion
description: "Before deleting TypeScript route ordering in Proxx, make proxy tests load the active CLJS runtime, route selected legacy-order tests through CLJS shadow policy, and keep local/auto flows fail-open until corresponding CLJS clauses exist."
license: GPL-3.0
metadata:
  origin: session-mycology-promotion
  promoted-from-spore: cljs-policy-test-harness-before-route-order-deletion
  recurrence: 2
---

# Skill: cljs-policy-test-harness-before-route-order-deletion

## Goal
Before deleting TypeScript route ordering in Proxx, make proxy tests load the active CLJS runtime, route selected legacy-order tests through CLJS shadow policy, and keep local/auto flows fail-open until corresponding CLJS clauses exist.

## Use This Skill When
- The same pattern or failure mode has recurred enough to deserve a named protocol.
- The current task clearly matches the lesson captured by this promoted spore.

## Do Not Use This Skill When
- The situation is obviously unrelated to cljs-policy-test-harness-before-route-order-deletion.
- You only have a one-off glitch with no evidence that the recurring pattern applies.

## Inputs
- The current task context.
- The relevant files, logs, or artifacts that exhibit the pattern.

## Steps
1. Verify the current task really matches the recurring pattern.
2. Apply the core lesson from the originating spore: Before deleting TypeScript route ordering in Proxx, make proxy tests load the active CLJS runtime, route selected legacy-order tests through CLJS shadow policy, and keep local/auto flows fail-open until corresponding CLJS clauses exist.
3. Prefer concrete evidence over narrative momentum.
4. If the pattern no longer fits reality, update or retire this skill instead of forcing it.

## Output
- A truthful, concrete application of the pattern to the current task.
