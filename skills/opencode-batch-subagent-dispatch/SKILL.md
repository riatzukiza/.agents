---
name: opencode-batch-subagent-dispatch
description: "Running opencode subagents as batch workers from babashka: mode:primary+hidden:true required for --agent CLI dispatch (subagent mode falls back silently); babashka.process must pass :in \"\" (stdin EOF or the CLI hangs) and read :out/:err from the derefed process map, not the proc handle; extract structured JSON from the --format json event stream last-text-part-first with a brace-substring fallback; model kimi-for-coding/k3 works when opencode/zen credits are absent."
license: GPL-3.0
metadata:
  origin: session-mycology-promotion
  promoted-from-spore: opencode-batch-subagent-dispatch
  recurrence: 1
---

# Skill: opencode-batch-subagent-dispatch

## Goal
Running opencode subagents as batch workers from babashka: mode:primary+hidden:true required for --agent CLI dispatch (subagent mode falls back silently); babashka.process must pass :in "" (stdin EOF or the CLI hangs) and read :out/:err from the derefed process map, not the proc handle; extract structured JSON from the --format json event stream last-text-part-first with a brace-substring fallback; model kimi-for-coding/k3 works when opencode/zen credits are absent.

## Use This Skill When
- The same pattern or failure mode has recurred enough to deserve a named protocol.
- The current task clearly matches the lesson captured by this promoted spore.

## Do Not Use This Skill When
- The situation is obviously unrelated to opencode-batch-subagent-dispatch.
- You only have a one-off glitch with no evidence that the recurring pattern applies.

## Inputs
- The current task context.
- The relevant files, logs, or artifacts that exhibit the pattern.

## Steps
1. Verify the current task really matches the recurring pattern.
2. Apply the core lesson from the originating spore: Running opencode subagents as batch workers from babashka: mode:primary+hidden:true required for --agent CLI dispatch (subagent mode falls back silently); babashka.process must pass :in "" (stdin EOF or the CLI hangs) and read :out/:err from the derefed process map, not the proc handle; extract structured JSON from the --format json event stream last-text-part-first with a brace-substring fallback; model kimi-for-coding/k3 works when opencode/zen credits are absent.
3. Prefer concrete evidence over narrative momentum.
4. If the pattern no longer fits reality, update or retire this skill instead of forcing it.

## Output
- A truthful, concrete application of the pattern to the current task.
